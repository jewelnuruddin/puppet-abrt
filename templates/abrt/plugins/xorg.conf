# List of modules which, when loaded, should make
# Xorg crashes non-reportable.
# Names should be without "_drv.so" suffix:
BlacklistedXorgModules = <%= scope['abrt::addon::xorg::blacklistedXorgModules'] %>
